extends layout

block content
    div.w3-container.w3-gray
        h1= title

    //ul
    //    each teacher in teacher_list
    //        li
    //            a(href=teacher.url) #{teacher.name}
    //            |  (#{teacher.id_number} - #{teacher.email_address})
    //
    //    else
    //        li Seznam vyučujících je prázdny.

    table.w3-table-all.w3-margin(style="width:95%")
        thead
            tr.w3-light-gray
                th ID
                th Jméno
                th Email
        each teacher in teacher_list
            tr
                td #{teacher.id_number}
                td
                    a(href=teacher.url) #{teacher.name}
                td #{teacher.email_address}

        else
            tr
                td Seznam vyučujících je prázdný